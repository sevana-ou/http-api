build_exe:
  script:
    - "echo $PATH"
    - "mkdir -p build"
    - "cd build"
    - "cmake ../test/server"
    - "cmake --build ."
    - "cd .."
    - "rm -rf build"
    - "mkdir -p build"
    - "cd build"
    - "cmake ../test/client"
    - "cmake --build ."
    - "cd .."
    - "rm -rf build"
